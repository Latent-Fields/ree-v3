## Effort-dissociating environment (Q-080) (2026-07-09)
- `CausalGridWorld` env extension (`ree_core/environment/causal_grid_world.py`) -- IMPLEMENTED 2026-07-09.
  Dissociates EFFORT from BENEFIT for open_question Q-080 (`ethics_engine_3.effort_as_harm_energy_conservation`); serves the two load-bearing sub-questions Q-080.a (harm-coupling) + Q-080.b (least-effort prior). NOT a mechanism/SD claim -- mints nothing; the factorial ablations answer Q-080.
  Config (env-only kwargs, NOT surfaced through `REEConfig.from_dims` -- SD-047/048/049/054 precedent): `effort_dissociation_enabled` (default False; master switch, bit-identical OFF), `effort_base_cost` (0.01), `effort_high_multiplier` (3.0), `effort_low/high_col_offset` (2/2), `effort_exertion_threshold/accrual_rate/recovery_rate/ratchet_mark/ratchet_rate` (0.02/0.02/0.002/0.5/0.005), `effort_harm_coupling_enabled` (False; **Q-080.a ON factor**) + `effort_harm_coupling_scale/depletion_weight` (1.0/1.0), `effort_benefit_asymmetry` (0.0 = tied probe; **Q-080.b control lever**).
  Layout: walled two-corridor grid, a LOW-effort (cost x1.0) and a HIGH-effort (cost x`effort_high_multiplier`) vertical corridor of EQUAL length joining a bottom agent-start junction to a top single shared-resource junction -- equidistant so the dissociation is purely energetic (isolated from ARC-068 time). Data flow: per-cell `_effort_cost_grid` -> per-action energy drawdown on top of `energy_decay` (HIGH drains ~3x faster) -> chronic-exertion accumulator (`_exertion_load` slow-recovery + `_exertion_permanent` irreversible ratchet). When `effort_harm_coupling_enabled`, per-step effort + `effective_depletion` are folded into `harm_exposure` + `harm_obs_a_ema` so z_harm_a (=||harm_obs_a||) carries effort -> SD-032e pACC / MECH-219 (source-agnostic scalar integrators) + residue see it with ZERO agent-code change (the "SD-032e effort-input variant" realised env-side).
  Observables (obs_dict + info, present only when enabled -> flat obs dims unchanged, bit-identical OFF): `effort_cost_by_action[5]` (per-candidate cost at SELECTION time -- what a least-effort prior/value-subtraction term reads), `effort_cost_this_step`, `exertion_load`, `exertion_permanent`, `effort_depletion`, `effort_corridor`, `effort_energy_cumulative`, `effort_harm_injected`.
  Precondition: requires `use_proxy_fields=True` (ValueError otherwise). No phased training (env extension only, no new encoder/training target). Consumers exercised (all pre-existing, unchanged): SD-032b `use_dacc`, SD-032e `use_pacc_analog`, SD-032c `use_aic_analog`, SD-012 drive, MECH-320 `use_tonic_vigor`, MECH-219 `use_harm_suffering_accumulator`.
  Smoke-tested 2026-07-09 (all 3 non-degeneracy requirements + irreversibility + coupling contrast + tied-probe); env contracts + preflight green. Validation experiments: V3-EXQ-<Q-080.a>, V3-EXQ-<Q-080.b> queued (see experiment_queue.json).
  Spec: `REE_assembly/docs/architecture/effort_dissociation_env.md`. Claim: Q-080 (open) in `REE_assembly/docs/claims/claims.yaml`.
