{
  "schema_version": "v1",
  "calibration": {
    "ms_per_episode_condition": 8000,
    "hardware": "Apple M2 Air 8 GB",
    "calibrated_at": "2026-03-16",
    "calibration_note": "With hazard_harm=0.02, avg episode ~182 steps (was ~18 with 0.5). Actual episode time higher."
  },
  "items": [
    {
      "queue_id": "V3-EXQ-1023",
      "title": "SD-106 validation: does generic bottleneck variance preservation reach PCA-32 parity at the consumer rung?",
      "script": "experiments/v3_exq_1023_sd106_bottleneck_preservation_validation.py",
      "priority": 8,
      "machine_affinity": "any",
      "estimated_minutes": 400,
      "claim_id": "SD-106",
      "note": "SD-106's pre-set acceptance measurement, per /implement-substrate Step 8. ACCEPTANCE TARGET (set by governance gov-20260911, NOT invented at build time): the observation->z_world latent reaches PCA-32 parity -- >= 0.85 held-out oracle-action agreement at the consumer rung (x734.PPOPolicyNet at PPO_TRUNK_HIDDEN) on a seed majority. The instrument is IMPORTED FROM V3-EXQ-1010, never re-implemented: capacity ladder, dataset recipe, standardiser, fit protocol, calibration anchor and thresholds all come from x1010/x1008/x1002, and x1010._fit_track_cell performs every cell. The one addition is a fourth track whose encoder trained with the SD-106 objective (preservation_weight=200.0, use_world_encoder_skip=True). 1010 itself is unmodified. | red-team (opus-5; fable requested first, refused by a provider spend limit, re-spawned once on the session model per Step 4.5): CONTESTED -> all 5 findings FIXED, 0 dismissed. F1 the anchor reachability certificate scored best-over-rungs while the gate measured the consumer rung, and the shipped `min` predicate was unmeetable against 1010's own anchor data (0.8578 < 0.85) -> references corrected and the predicate made a seed-majority rule; F2 a None `measured` made p0_readiness_gate raise an UNCAUGHT TypeError on the instrument-failure path, converting the designed substrate_not_ready_requeue self-route into an ERROR with no manifest -> guarded; F3 the C2 non-degeneracy witness (|delta| > 1e-9) could not fail and C1's own non-degeneracy was built on it -> C2 is now the mechanism fact (preservation head ran on every ON cell, no OFF cell); F4 1010's per-arm encoder-health gates were dropped -> encoder-trained and not-collapsed preconditions restored, unused ANCHOR_DEGRADE_TOL removed rather than advertised; F5 three minor (invented reference value, always-null offending_cell, unreachable branch) -> fixed. A sixth defect was found while fixing F4: the restored weight-delta gate read a non-existent key and would have failed forever. | Step 2.4 GOV-REUSE-1: the decisive readout is oracle-action agreement on an SD-106-ON latent; SD-106 landed 2026-09-11, so no recorded manifest on any substrate_hash can carry it -- not recoverable, run required. | Step 2.5c SCOPED USER EXCEPTION 2026-09-11: three open `corrupting` substrate entries overlap modules any all-ON agent driver exercises -- mode-governance-engagement (ree_core/agent.py, utils/config.py, salience_coordinator.py), contextmemory-write-path-addressing-degeneracy (e1_deep.py::ContextMemory.write) and SD-e1-rollout-consistency-training (e1_deep.py::forward, ::predict_long_horizon). Granted because V3-EXQ-1010, the run governance pre-set as this acceptance measurement, ALREADY EXECUTED under these identical three defects, and this comparison is PAIRED (ON vs OFF vs anchor vs raw, all under the same defects). Recorded so the manifest and any later autopsy see the limitation. | PROMOTES NOTHING on its own; experiment_purpose=diagnostic. A null means the objective at preservation_weight=200.0 does not recover oracle-relevant content on this rung -- NOT that preservation is the wrong lever -> /failure-autopsy, not an automatic re-queue at another weight.",
      "status": "claimed",
      "claimed_by": {
        "machine": "ree-cloud-2",
        "claimed_at": "2026-09-11T17:25:49Z"
      },
      "conditions": 16,
      "episodes_per_run": 290,
      "experiment_type": "v3_exq_1023_sd106_bottleneck_preservation_validation",
      "seeds": 3
    },
    {
      "queue_id": "V3-EXQ-964a",
      "title": "MECH-482/SD-102 epistemic-deficit accumulator: multi-target readiness validation (supersedes V3-EXQ-964)",
      "script": "experiments/v3_exq_964a_mech482_epistemic_deficit_multitarget_readiness.py",
      "priority": 5,
      "machine_affinity": "any",
      "estimated_minutes": 25,
      "supersedes": "V3-EXQ-964",
      "claim_id": "MECH-482",
      "note": "Validates substrate_queue entry sd_epistemic_deficit_multitarget_readiness (implemented_pending_validation, severity degrading) -- the entry this run is the owed validation for. Owed residual of GFLAG-0215 (resolved; registry half landed in gov-flagbacklog-20260909). DESIGN: epistemic_deficit is ARMED ON BOTH ARMS (the V3-EXQ-964 defect was arming it on one arm only); the manipulation is the readiness GEOMETRY alone, all five levers incl. target_frame=predicted. 964's C2 was structurally unsatisfiable -- n_targets==1 made the readout candidate-constant, and a uniform constant cannot move an argmax, so its divergence of 0.0 was an arithmetic identity, not a measurement. C3 here is gated behind C1 (n_targets>=2), C2 (cross-candidate range > 0) and a MAGNITUDE gate measured post-F against E3.decisiveness_margin, so an unmet gate self-routes substrate_not_ready_requeue rather than any substrate verdict. GOV-REUSE-1 (Step 2.4): checked v3_exq_964_mech482_epistemic_deficit_validation_20260829T215030Z_v3 -- it carries the decisive readouts (n_targets_min 1, yoked_divergence_frac_max 0.0) but only for the PRE-readiness substrate; the readiness knobs post-date that substrate_hash, so the readiness question is not recoverable from it -> run required. The broadcast-vs-epistemic_deficit contrast is NOT re-run; 964 already records it. RED-TEAM (Step 4.5) model: Opus 5 (session model; the fable override returned a spend-limit error, skill-sanctioned fallback). Verdict BLOCKING -> fixed before queueing: (1) epistemic_deficit_target_frame, the FIFTH readiness lever, was omitted -- under the 'realized' default no radius can both separate targets and match candidates (config.py:4733 measured note); (2) the reachability figure double-multiplied by curiosity_learning_progress_weight, since _last_lp_dev_range is computed from the already-weighted lp_contrib (structured_curiosity.py:548) -- was 20x understated; (3) accumulator counters are cleared by reset() every episode even under persist=True (epistemic_deficit.py:616-633), so run-lifetime totals are now accumulated per-episode; (4) vacuous rate was denominated on non-refused reads only, since mark_vacuous_readout never increments _n_readouts -- now denominated on all attempts; (5) C4 (instrument control) promoted to load-bearing, as a non-zero self-yoked divergence on EITHER arm voids the paired DV. DISPOSITIONED, not fixed: the reviewer wanted a binding gate on matched-target count; declined with citation -- under readout_mode=rbf_weighted the returned vector is a distance-weighted sum over ALL targets and the `matched` mask is never consulted (epistemic_deficit.py readout(), rbf branch), so gating on it would manufacture a substrate_not_ready verdict from a readout that is in fact differentiating. Matched counts are recorded as diagnostics instead. KNOWN LIMITATIONS, declared: repeated runs at a fixed seed are NOT bit-reproducible (the predecessor is), so every cell is stamped reuse-INELIGIBLE and must never be used as a baseline mint; within-run yoking IS exact (paired control 0.000000 on every observed run) and no load-bearing criterion depends on cross-run reproducibility. Yoking is non-compounding only while the arms agree -- the env is stepped with the reference arm's action. 1-seed full-scale probe: max_n_targets 13 (vs 1 pre-readiness), lp_dev_range 0.0129, distinct-matched 0, pert/margin 0.111, divergence 0.0 -> would route 'mixed' / readout_differentiates_but_never_changes_committed_action, which is a genuine measurement rather than 964's arithmetic zero. Step 2.5c: the one open corrupting entry overlapping this driver (mode-governance-engagement) matches only at module level on agent.py; its defect is the mode-hysteresis path and this experiment sets no mode-governance config -> not a stop.",
      "status": "pending",
      "conditions": 1,
      "episodes_per_run": 3,
      "experiment_type": "v3_exq_964a_mech482_epistemic_deficit_multitarget_readiness",
      "seeds": 3
    },
    {
      "queue_id": "V3-EXQ-1025",
      "title": "MECH-349 CandidateRuleField churn/retirement leg: is churn selective, or the 666c treadmill?",
      "experiment_type": "v3_exq_1025_mech349_crf_churn_retirement",
      "script": "experiments/v3_exq_1025_mech349_crf_churn_retirement.py",
      "priority": 8,
      "machine_affinity": "any",
      "status": "pending",
      "estimated_minutes": 2,
      "episodes_per_run": 840,
      "seeds": 5,
      "conditions": 4,
      "claim_id": "MECH-349",
      "note": "MECH-349 FALSIFYING(3), the churn/retirement leg V3-EXQ-1024 explicitly could NOT test: under the stack the claim names (mature_pool_dynamics + availability_maintenance, maintenance_decay=0.0) retirement is STRUCTURALLY UNREACHABLE, so crf_n_retired_total is pinned at 0 and the 666c signature cannot occur whatever the mechanism does. 1024 recorded that as a structural fact; GFLAG-0265 recorded the residual. This is that residual, and the option-C properly-tagged run MECH-349 evidence_quality_note owes. | LEVER, measured 2026-09-11: the remedy MECH-349 what_would_answer (c) names first -- maintenance_floor below mature_retire_floor -- is INERT on its own (_maybe_mint sets init_avail = max(tolerance_floor, maintenance_floor), so a rule still mints at 0.30 vs the 0.05 retire floor; retired=0 unchanged). Dropping tolerance_floor too DOES retire (93/100) but degenerately, every rule minting below the floor and dying at once, which MANUFACTURES the 666c signature instead of testing for it -- rejected. Lever used is crf_maintenance_decay, the only knob in credit() reaching a rule that carries no eligibility. Single-axis arms 0.00 anchor / 0.02 / 0.10 scored / 0.35 positive control; selective plateau measured 0.01-0.28, treadmill from 0.30, so the scored arms are interior to a 28x basin. | SCOPE CORRECTION recorded, not scored: MECH-349 non-degeneracy (b) asks retired/minted <= 0.25, but in an ecology containing extinct regularities that ratio is fixed by the ecology -- a perfectly SELECTIVE creator scores 0.50 and would FAIL the bar for behaving correctly. Criteria are per-regime-class instead. | Step 2.4 GOV-REUSE-1: decisive readout is crf_n_retired_total / persistent-vs-extinct churn at a retirement-reachable operating point. Only 4 manifests carry crf_n_retired_total (666, 666a, 666b, 666c), all June 2026 on four distinct substrate_hashes and all PRE-SD-078, while FALSIFYING(3) asks specifically about the SD-078-CENTERED key -- not recoverable, run required. | Step 2.5b re-derive brake: 0 counted autopsies on MECH-349, not braked. Step 2.5c: no open corrupting substrate_queue entry overlaps ree_core/policy/candidate_rule_field.py (the driver imports only that module + torch). | red-team (opus-5; fable requested first, refused by a provider spend limit, re-spawned once on the session model per Step 4.5): CONTESTED -> all findings FIXED, none dismissed. F1 C2 was ANALYTICALLY INERT, implied by (gate AND C1) since the gate forced every scored cell to retire >=1 while C1 forced retired_persistent==0; F2 the readiness gate EVICTED an arm on C2 own predicate before C2 ran, so a dose retiring nothing was dropped rather than failing -- narrowing always toward PASS, and it FIRED in the smoke; fixed together by gating reachability on the POSITIVE CONTROL only, which makes the crf_retirement_not_exercised branch reachable (the same smoke now correctly reports it). F3 non_degenerate could never be False because the anchor arm is unconditionally green -> gated on a scored arm, with the full per-arm precondition list emitted when none is. F5 MECH-349 (c) was half-implemented and its missing conjunct failed on every cell (all 12 mints in ticks 24-35 of 840) -> ecology now staggers persistent-regime onsets and the conjunct is gated; 2 late mints per arm/seed after the fix. F6 minor: anchor could not fail, NaN reached the manifest, realized-n seed denominator -> all fixed. | SEPARATELY, found pre-review and independently confirmed by it: churn bookkeeping by set-difference on slot indices undercounted 30-47% because credit() retires before _maybe_mint() and _free_slot_index() reuses the freed slot in the SAME step, and id() cannot detect it (CPython address reuse) -> rules keyed (slot, minted_step) with hard reconciliation and partition asserts. | SEEDS ARE A REPRODUCIBILITY GATE, NOT POWER: regime directions are seed-independent so DVs are near-deterministic; the discrimination is BETWEEN ARMS (0 vs 6 vs 165 retirements). The dose-response was pre-measured at design time, so a supports here is a properly-tagged filing, not an independent discriminating test. | MECH-350/351/352 NOT tagged; crf_max_pairwise_rule_dist is a structural identity here and is recorded for continuity only."
    }
  ]
}
