{
  "schema_version": "v1",
  "calibration": {
    "ms_per_episode_condition": 8000,
    "hardware": "Apple M2 Air 8 GB",
    "calibrated_at": "2026-03-16",
    "calibration_note": "With hazard_harm=0.02, avg episode ~182 steps (was ~18 with 0.5). Actual episode time higher."
  },
  "items": [
    {
      "queue_id": "V3-EXQ-1043",
      "title": "MECH-537 communication-subspace routing -- is the oracle's decision content ENCODED BUT NOT EXPOSED? (RRR world_state -> z_world, rank-matched random control)",
      "script": "experiments/v3_exq_1043_mech537_communication_subspace_routing.py",
      "priority": 40,
      "machine_affinity": "any",
      "estimated_minutes": 480,
      "claim_id": "MECH-537",
      "backlog_id": "EVB-1729",
      "note": "DIAGNOSTIC (excluded from governance confidence/conflict scoring). Tests MECH-537 (communication-subspace routing failure: 'encoded but not exposed') per its registered what_would_answer and proposal EXP-1403 (manual, thought-digestion-20260915 wave 3, user-approved; auto twin EVB-1729/EXP-0133). READ-ONLY over a FROZEN encoder: no ree_core change, no new substrate, no behavioural DV, no commitment layer -- this is the commitment-free measurement-only diagnostic the IGW brief prefers. | DESIGN: sender X = the full 250-dim world_state (the tensor the encoder ACTUALLY reads); receiver Y = sense()-time z_world (the tensor the consumer ACTUALLY reads); P_comm estimated by experiments/_lib/interface_probe.communication_subspace (RRR, rank by grouped-by-episode cross-validation on the TRAIN episodes only). Four arms, all fed a 250-dim tensor so in_dim and decoder capacity are identical by construction: ws250_full / ws250_comm (P X) / ws250_perp ((I-P) X) / ws250_randrank (random subspace of the SAME rank). z_world is DISQUALIFIED as a sender by V3-EXQ-1010's H-F-confirmed verdict and is not used as one. | WHY THE FULL world_state RATHER THAN rawfield25: the 25-dim resource field is literally world_state[225:250] (asserted at runtime by --self-test), so a complement computed inside it would exclude 90% of the encoder's input from the complement -- and a routing claim measured on 10% of the sender is not a routing claim about the sender. It is also what makes the causal leg well-posed: world_state is an argument to agent.sense(), resource_field_view is not. | Step 2.4 GOV-REUSE-1: decisive readout = held-out oracle-action decodability from P_comm X vs from the full sender vs from the complement, at matched rank. No manifest records any communication-subspace projection readout -- the instrument (interface_probe.py) was only built 2026-09-10 and its own docstring states the assay scripts are 'a later /queue-experiment build, not this one'. Checked 1002/1008/1010 manifests and reanalysis_query for comm_subspace/RRR readouts: absent -> not recoverable, run. | Step 2.5 / 2.5a: MECH-537 is not v3_pending; interface_probe.communication_subspace, principal_angles and the x1002 dataset/decoder protocol all exist. Runtime probe confirmed the premise: world_state 250-dim, z_world 32-dim, resource_field_view == world_state[225:250], sense() reachable, z_world responds to observation change. SD-080 is NOT implemented, so that alternative locus from EXP-1403 is unavailable; the primary source needs no substrate at all. | Step 2.5b re-derive brake: MECH-537 counted 0 braking autopsies (registered 2026-09-08, no experiments yet). Not braked. | Step 2.5c substrate-path overlap gate: four open corrupting-severity substrate_queue.json entries exist (MECH-320 tonic_vigor.py; contextmemory-write-path-addressing-degeneracy e1_deep.py::ContextMemory.write; sd_blocked_agency_mismatch_floor_calibration affect/blocked_agency.py; sd105_frozen_shared_entropy_floor_multiplier regulators/selection_entropy_floor.py). NOT BLOCKED, reasoning recorded in the driver docstring so a later autopsy can disagree with it rather than guess: (a) all four are POLICY/AFFECT/E1-MEMORY modules exercised only in the WARMUP that produces the frozen encoder, never by the MEASUREMENT (a supervised decode from stored observations to ORACLE labels, plus frozen forward passes through sense() -- the agent's own policy appears in no readout); (b) the object of study is the 978-OFF frozen encoder AS IT IS, the same object 1002/1008/1010 studied and whose H-F verdict is the load-bearing input to EXP-1403's own design, and the question is asked of whatever encoder the warmup produces under this run's own in-run adequacy gate; (c) three of the four are already implemented*-pending-validation (fixed, awaiting confirmation, not live) and the fourth is by its own title a constraint on 'a difference-of-arms design whose DV is the entropy it regulates' -- this run's DV is decode agreement and encoder sensitivity and it arms no entropy set-point, so it is inapplicable by its own terms. DEGRADING entries that DO touch the measurement path, recorded per the gate's degrading rule: SD-018 and SD-106 (latent/stack.py, latent/zworld_p0.py, agent.py::compute_resource_proximity_loss -- the observation->z_world bottleneck, i.e. precisely the mechanism under test), SD-MECH303-THRESHOLD-SOURCING and mech357-freeze-incompatible-pressure-mechanism (environment/causal_grid_world.py). | Step 2.6 ethics preflight: all flags false, decision allow (SENT-0, routine V3). | DV-SYMMETRY (mandatory, per arm): every arm's DV is held-out top-1 agreement between a trained decoder's argmax over 5 action logits and the oracle's action; its symmetry group is a uniform additive constant across the logits, any monotone rescaling of them, and a permutation of interchangeable candidates. ws250_full / ws250_comm / ws250_perp / ws250_randrank: the manipulation is a change of the LINEAR SUBSPACE the decoder's input is confined to, which is invariant under none of the three -- a projection changes the input's information content state-by-state, so the induced logit change is state-dependent, not a broadcast constant, not a monotone map, not a relabelling; two of the four also differ in input-subspace RANK and all four differ in retained norm on the five oracle decision coordinates (both measured and recorded). Causal leg DV is ||dz_world||, a norm, whose symmetry group is rotations of z_world and permutations of the perturbation draws; the manipulation is WHICH SUBSPACE the perturbation component is taken from, which is neither. | SMOKE: --dry-run PASS end-to-end (no crash; 'ep N/M' progress lines with M = p0+p1, one 'Seed S Condition C' boundary per seed, one 'verdict:' line per seed, manifest written and relocated, sentinel emitted). Correctly self-routed FAIL/substrate_not_ready_requeue at dry-run scale because the source-adequacy precondition refused -- the gate working. validate_experiments.py --strict: OK, 0 warnings. validate_recording.py --strict on the dry-run manifest: OK, 1 complete, 0 always-core gaps, 0 flat-scalar-readout findings, 0 criteria-re-derivability findings. --self-test PASS (23 assertions: every verdict-grid branch, the falsification predicate, basis/projection algebra, and the three runtime premises). ASCII-only confirmed (0 non-ascii lines). | TWO SMOKE-DRIVEN DEFECTS FOUND AND FIXED BEFORE QUEUING, both of which would have wasted the full run: (1) torch.linalg.solve raised 'input matrix is singular' -- world_state is largely a 5x5x7 one-hot local view, so ~114 of its 250 dims are exactly constant on a split and land exactly at zero after the train-split z-score, and the instrument's default absolute ridge 1e-6 is eight orders of magnitude below the other diagonal entries. Fixed by a RELATIVE ridge scaled to mean(diag(Xc^T Xc)) plus dropping the exactly-constant dims from the RRR FIT ONLY and embedding the fitted basis back into the full 250-dim ambient space with zero rows (so every arm still presents 250 dims and the dropped dims land, correctly, in the complement). Live-dim count measured 136, matching 1008's independently recorded 'numeric rank 135' for this sender. (2) cross-validation SATURATED at the instrument's default rank ceiling of min(16,dx,dy)=16, i.e. the ladder rather than the data was choosing the rank, which would have made 'the RRR rank selected by cross-validation' (the claim's own operationalisation) untrue of this run. Fixed by extending the ladder to 1..32 = 1..dy, the whole mathematically meaningful range (at rank dy the rank constraint is inactive and the fit IS unconstrained OLS, so a selection at 32 is a genuine CV verdict); rrr_rank_at_ladder_ceiling recorded per seed. | red-team (fable, TWO passes -- the first pass's BLOCKING findings changed the causal chain itself, which is the one condition the skill permits a single re-spawn on): PASS 1 BLOCKING, 7 findings. F1+F2 BLOCKING, both FIXED: (F1) C4 drew complement directions isotropically, so ~98% of every perturbation's energy sat on coordinates the claim says nothing about and C4 measured 'is the encoder Jacobian isotropic off the subspace' rather than 'does the decision content reach the receiver' -- fixed by AIMING the probe at the five coordinates the oracle actually reads (x1008's DECISION_WORLD_STATE_INDICES {232,236,237,238,242}), splitting each into its comm and complement components; (F2) a driver-invented sensitivity-based premise override could fire while the bases were demonstrably stable and the RRR explained the receiver at R^2~0.999, emitting an unattributable non_contributory -- WITHDRAWN entirely, the premise route is now the stability falsifier the claim actually registers. F4 CONFIRMED against source and FIXED: the 'full-rank ceiling' reachability precondition compared the driver's PER-COLUMN R^2 against interface_probe._r2's GLOBAL-mean R^2, which is why the smoke showed RRR 0.9991 above its own 'ceiling' 0.9156 -- precondition dropped as ill-founded (the gate is self-anchoring: its measured value is the instrument's own best CV score over the whole ladder) and the non-comparability is now stated in the manifest. F3/F5/F6/F7 CONTESTED, all FIXED: C3 restated as rank-confound-free retention (D_full - D_perp <= tol) and marked not-load-bearing since a pass is near-automatic by dimensionality and only its FAILURE is informative; C4's decomposition moved into the standardised basis so C3's and C4's complements are the same subspace, with the diag(std) rotation now measured; BOTH random controls redrawn inside the LIVE dims and embedded so their support matches the fitted basis exactly (drawn over all 250 they wasted ~46% of their rank on identically-zero dims, handicapping ARM_RAND and biasing C2, the load-bearing criterion); cross-stratum instability now counts only where the two strata's data spans overlap, measured by principal angles on their own top-r principal directions. Also added from the pass 1 branch-coverage note: a SECOND, reachable falsification (C1 with a non-positive orientation contrast -> weakens, 'the drop is rank, not orientation'), since the claim's own registered falsifier is unlikely to be reached on this source and a grid that can confirm but not falsify is not a test. PASS 2 BLOCKING, 4 findings, all FIXED: (R2-F1) the new falsification branch keyed on `not C2`, and since C2 is a conjunctive positivity test (floor AND 2*SD AND seed-majority) its negation is satisfied by data positive on EVERY seed -- e.g. [0.10,0.30,0.02] -- so the run could have printed a falsification whose text asserted the opposite of the measured sign; fixed with a dedicated positive predicate _c2_falsified (mean <= 0 AND a seed majority <= 0), the branch additionally requiring `not c2`, and three new self-test assertions pinning exactly that shape to 'undetermined'. (R2-F2) cross-SEED basis overlap was an unconditional premise input, but each seed warms up its OWN agent, so it compared the communication subspaces of THREE DIFFERENT TRAINED ENCODERS -- encoder non-identifiability across training replicates, not the receiver-state-strata instability the claim registers, and below the 0.50 bar largely by construction (chance overlap of two independent rank-32 subspaces in 136 dims is ~0.24), so a run with a unanimous per-seed routing signature could have emitted premise-fails/non_contributory; fixed by removing cross-seed from the premise (recorded as a diagnostic) and scoring the within-encoder cross-stratum test against the MEASURED chance level for this rank and live-dim count plus a margin, rather than an absolute bar that silently changes meaning with the CV-selected rank. (R2-F3) C4 normalised both components to unit raw norm, discarding the weights a,b in e_j = raw_in + raw_out -- and since the consumer's response is a*J(u_in) + b*J(u_out), a unit-normalised ratio is deflated by b/a exactly where comm retention is low, which is the regime in which C1/C2 pass, so a confirm could be printed while the complement path carried 1.5-3x the comm path; fixed by perturbing with the UN-NORMALISED components (weights inside the measurement) and recording the per-index component norms, and C4a (the null-margin leg) DEMOTED out of the confirm path to a recorded diagnostic because a random subspace is not matched on retention once the weights are in -- C4 is now the absolute un-normalised ratio alone, control-free and reachable by hypothesis (ratio -> ~0 under a genuine routing failure, ~b/a > 1 otherwise; the smoke measured 6.46 at that no-routing end). (R2-F4) C3's non-degeneracy flag was computed on C1's deltas -- fixed. Items the pass-2 reviewer checked and found SOUND: no leakage in the RRR-then-decoder ordering (standardiser and RRR fitted on train episodes only, grouped CV, applied as a fixed linear map to held-out episodes); realized-vs-intended n correct; rrr_heldout_r2 self-anchoring; randrank_control gate protective in the conservative direction; all five verdict branches reachable; C1/C2/C3/C4/C5 jointly satisfiable at plausible substrate values. | STATED LIMITATIONS, not papered over: (a) the causal leg probes the FEEDFORWARD path only (measured at the canonical post-reset recurrent state, deliberately, so the top-down term is held fixed across conditions) -- a complement direction acting solely through the recurrent term would be missed, which is why the trajectory-based variance-routing readouts are recorded alongside as corroborating diagnostics and never as criteria; (b) C2's reachability degrades if cross-validation selects a LOW rank (a random rank-10 subspace decodes near the trivial predictor, leaving C2 little room), so selected_rank is recorded per seed and a low-rank run's C2 must be read with that in mind; (c) C4b's 0.50 ceiling is the least externally-anchored threshold in the design and its per-seed values are recorded so a later autopsy can re-adjudicate it. | Runtime estimate 480 min is calibrated off V3-EXQ-1002 (314 min on a cloud worker for 3 warmups + 9 mlp128 decoder fits); this run is 3 warmups + 12 fits at in_dim 250, plus a closed-form RRR and ~25k frozen sense() calls. Landed without its two parallel-session neighbours: untracked v3_exq_1044 and v3_exq_1046 scripts from other sessions were present in the shared checkout at authoring time and were deliberately NOT staged (pathspec-limited ree_commit.py, never `git add experiments/`); their queue entries were confirmed absent from both HEAD and the working tree before appending.",
      "status": "claimed",
      "claimed_by": {
        "machine": "ree-cloud-2",
        "claimed_at": "2026-09-16T11:16:29Z"
      },
      "conditions": 1,
      "episodes_per_run": 290,
      "experiment_type": "v3_exq_1043_mech537_communication_subspace_routing",
      "seeds": 3
    }
  ]
}
