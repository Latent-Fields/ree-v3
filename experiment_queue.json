{
  "schema_version": "v1",
  "calibration": {
    "ms_per_episode_condition": 8000,
    "hardware": "Apple M2 Air 8 GB",
    "calibrated_at": "2026-03-16",
    "calibration_note": "With hazard_harm=0.02, avg episode ~182 steps (was ~18 with 0.5). Actual episode time higher."
  },
  "items": [
    {
      "queue_id": "V3-EXQ-967",
      "title": "MECH-144 shuffle-inertness confirmer: why were V3-EXQ-966's arms behaviourally identical?",
      "script": "experiments/v3_exq_967_mech144_shuffle_inertness_confirmer.py",
      "priority": 90,
      "machine_affinity": "any",
      "estimated_minutes": 90,
      "note": "Cheap confirmer for V3-EXQ-966, run FIRST per the user's Step 8 ratification of confirmed autopsy failure_autopsy_966-436g-951-959-822d-cluster_2026-08-30 (REE_assembly 40dd17331e; governance-ratified 54dbe477be) at 2026-08-30T15:41:56Z: 'the cheap confirmer runs FIRST -- one seed, blocks 3-4, both arms, logging amplitudes, agent_energy and per-step actions -- before any substrate work is specified or 966a is queued.' This entry does NOT redesign 966 and does NOT specify a substrate build. experiment_purpose=diagnostic, claim_ids=[] -- it localises an instrument, it tests no claim, and is excluded from governance confidence scoring. | STEP 2.4 (GOV-REUSE-1): decisive readout is the MATCHED-STEP divergence of agent_energy and of the committed action index between ARM_FIXED_VALUE and ARM_SHUFFLED_VALUE. Checked run v3_exq_966_mech143_144_hippocampal_value_sensitivity_causal_20260830T115037Z_v3 (substrate_hash 210a5c861e6113993e5f, 8 arm_results): its rows carry only block-level aggregates (block_contacts, frac_high_pre/post, n_contacts_pre/post, c1) and NO per-step trace, so the readout is neither recorded nor derivable -> run. | STEP 4.5 red-team (fable, run by predecessor session c1-queue-refill-20260901): CONTESTED with one BLOCKING -> all findings verified against source and APPLIED before queueing. BLOCKING: D2 compared distinct-benefit SETS and MEANS, which TIE under exact reproduction of 966's bit-identity (both arms visit both types, so each arm's set is {LOW,HIGH}), so the run would have emitted 'amplitude_not_reaching_consumption' and made the whole H-inert vs H-insensitive adjudication unreachable -- D2 is now MATCHED-STEP, aggregates retained as diagnostics. Reproduced live in this session's smoke: both arms report benefits=[0.045,0.3], i.e. the withdrawn aggregate form reads False exactly where the matched-step form reads True. HIGH: 966 trains e1+latent_stack for 12 P0 warmup episodes (Adam 3e-4, prediction + resource-encoder loss); this driver rolled the warmup with no optimiser (StepHarness train_mode only toggles the grad context), so its logged-block agent was untrained-encoder -- 966's P0 block is now replicated and a p0_warmup_actually_trained readiness precondition asserts it (smoke: 30 opt steps). MEDIUM: D1 and the applied benefit now read the LIVE env (env.resource_type_benefit_amplitudes) rather than driver constants. MEDIUM: energy divergence onset + extent recorded; C2_policy_responds is non-degenerate only when the post-divergence window clears MIN_POST_DIVERGENCE_STEPS=20, so H_insensitive_policy cannot fire off a near-zero exposure window. MINOR: per-cell verdict print keyed to MIN_LOGGED_CONTACTS; docstring cite :2131 -> :2133 (verified). | ADDITIONAL Step 3.5 finding by the queueing session, not from the red-team: D3 routes on agent_energy but nothing asserted a contact can MOVE agent_energy in this config, so a flat D3=False could have been an instrument artifact wearing the H_inert_in_fact substrate label. Added the contact_bump_reaches_agent_energy readiness precondition, which reconstructs contact_benefit from the observable channel alone and routes a dead channel to substrate_not_ready_requeue. Used as a LIVENESS FLOOR only -- it does not subtract the action/movement energy cost at causal_grid_world.py:2707, so its value is not a calibrated contact_benefit. Smoke reads bump_max=0.04 in BOTH arms, matching neither amplitude-derived benefit (0.3 / 0.045); if that survives the full run it is direct support for H_inert_in_fact_energy_route_blocked and is the first number to read. | DV-SYMMETRY (per arm, both arms share one DV set): the manipulation is a PERMUTATION of which resource type carries the HIGH amplitude. The DVs are (a) the per-step agent_energy trajectory, (b) the per-step committed action index, (c) PER-TYPE block_contacts. The manipulation is NOT invariant under any symmetry of these: (a) is amplitude-weighted, so a type permutation changes it whenever the two amplitudes differ (1.0 vs 0.15); (b) is compared step-matched, not as a set, so it is not a symmetric function of the type labels; (c) is reported per type and compared per type. Stated explicitly because the POOLED contact count IS invariant under the type permutation and frac_high is exactly antisymmetric under it -- that invariance is 966's original defect (frac_high_SHUFFLED = 1 - frac_high_FIXED by construction), and this driver deliberately reports none of those pooled/fractional forms. | Does NOT adjudicate chip-20260830-exq966-wantingweight-fromdims-dropsite: it sets wanting_weight via from_dims AND asserts the landed runtime value as a recorded precondition, so it is correct whichever way that chip resolves. Smoke PASS (structural; readiness correctly routes substrate_not_ready_requeue at dry-run length, which is expected and documented in ANCHOR_REACHABILITY_EXEMPT).",
      "status": "claimed",
      "claimed_by": {
        "machine": "ree-cloud-2",
        "claimed_at": "2026-09-01T06:05:49Z"
      },
      "claim_ids": [],
      "conditions": 2,
      "episodes_per_run": 52,
      "experiment_type": "v3_exq_967_mech144_shuffle_inertness_confirmer",
      "seeds": 1
    }
  ]
}
