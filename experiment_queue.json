{
  "schema_version": "v1",
  "calibration": {
    "ms_per_episode_condition": 8000,
    "hardware": "Apple M2 Air 8 GB",
    "calibrated_at": "2026-03-16",
    "calibration_note": "With hazard_harm=0.02, avg episode ~182 steps (was ~18 with 0.5). Actual episode time higher."
  },
  "items": [
    {
      "queue_id": "V3-EXQ-978",
      "title": "SD-018 AMEND validation: does supervising z_world on the DIRECTIONAL resource field lift a z_world-only reader's foraging competence? (+ fishtank observable)",
      "script": "experiments/v3_exq_978_sd018_directional_field_fishtank.py",
      "priority": 70,
      "machine_affinity": "any",
      "estimated_minutes": 600,
      "note": "OWED POST-IMPLEMENTATION VALIDATION of the SD-018 AMEND (directional resource-field head on z_world), landed ree-v3 028a625 2026-09-02, whose own commit message records 'Validation experiment NOT queued'. Routing chip chip-20260902-sd018-fieldhead-validation-run. EVIDENCE (not diagnostic): INV-088 primary, MECH-457 read-across only. | DESIGN: 2 arms + 2 anchors. field_loss_off / field_loss_on both BUILD the directional head and run the identical x734 all-ON stack; the ONLY difference is the P0a loss weight (zworld_p0_resource_field_weight 0.0 vs 0.5). Both arms' PPO readers see z_world ALONE (32 dims) -- the side-channel V3-EXQ-948 used is REMOVED, which is what this run contributes that 948 could not: 948 proved the field CONTENT is sufficient for competence by concatenating it onto the reader's input; it said nothing about whether that content can be carried THROUGH z_world. Env kwargs, rung (D3_hazard_free), weighting (W3_survival_zeroed), seeds, PPO hyperparameters, eval protocol and P0/P1 budgets are imported from x734/x808 rather than redefined. Both arms get their OWN warmup -- forced, because here the manipulation IS the encoder's training objective, so a shared warmup would erase it. | SEAM PREREQUISITE: the head was UNTRAINABLE from any driver in this family until 2026-09-02 (ZWorldP0Config.resource_field_weight defaults 0.0; run_zworld_p0 passed no override; _train_all_on_agent calls neither field loss). Seam LANDED ree-v3 65c37c8 (origin/main), pinned by contract C6 in tests/contracts/test_sd018_resource_field_head.py. chip-20260902-sd018-p0a-field-weight-seam. | GOV-REUSE-1 (step 2.4): no manifest under evidence/experiments/ carries resource_field_weight or used_resource_field_head -- the substrate did not exist until today, so nothing is recoverable by reanalysis. Run required. | red-team (fable): 8 findings, ALL APPLIED before queueing, none waived. F1 (confirmed by independent measurement) -- constructing resource_field_head consumes torch RNG draws and SplitEncoder builds it BEFORE world_topdown/self_topdown, so an arm pair differing in whether the head EXISTS also differs in the random init of modules that are never trained (world_path_parameters() = world_encoder + world_precision_logit only) yet feed sense()-time z_world directly: an arm-correlated untrained random term in the exact quantity the reader consumes. Fixed by building the head in BOTH arms. F8 -- p1 was passing P1_REINFORCE+P1_PPO (1090) where the family passes P1_REINFORCE (90), a 12x budget drift; fixed. F2/F5(doc) -- the manipulation is a REWEIGHTING not presence-vs-absence (reconstruction_weight 10.0 already reconstructs the full 250-wide world_obs INCLUDING the field at [225:250], so the OFF arm is supervised on it at 10/250 per element and the ON arm adds 0.5/25, a ~1.5x reweighting); the OFF arm's ABSOLUTE decode r2 is therefore as informative as the lift. F3 -- an uninformative branch was reporting dir_inv088='weakens'; the two null branches (P0a never moved z_world's field content; decode unmeasurable) are statements about THIS recipe and instrumentation, not about the hypothesis, and now report 'unknown', with a distinct label decode_unmeasurable_direction_unattributable separating an unmeasurable decode from a measured flat one. Extended to the partial-non-vacuity case: a RED ON arm emits no claim direction at all. F4 -- C_decode_lift required only the 0.05 margin, satisfiable between two sub-mean decodes (r2 -0.90 -> -0.85 is a lift of nothing); now also requires r_on > 0 absolutely. F5(code) -- new ON-arm precondition field_head_decodes_on_arm on P0a's OWN held-out field r2 > 0, so a head that RAN but LEARNED NOTHING reads as a failed operating point rather than as a scientific null about SD-018 (field_leg_ran is only a CONFIGURATION predicate). F6 -- encoder-path participation ratio and decode reported alongside the sense-time ones. F7 -- the fishtank trace logs the head's argmax on BOTH the sense-time z_world the policy consumes AND the encoder-path z_world P0a actually trains; sense-time z is (world_encoder(w) + world_topdown(...)) * prec while P0a trains world_encoder(w) * prec alone, so a head that tracks the gradient on the encoder path but not at sense time localises the failure to the top-down term -- the observation that would route a null to shape (b) rather than back to the P0a recipe. | FISHTANK: the user asked to be able to WATCH this. A separate, additional log pass (NOT the scored data -- the DV comes from evaluate_seed on its own fresh env) writes <run_id>_episode_log.json in the schema REE_assembly/fishtank_viz.html renders, carrying per step resource_field_true_argmax, resource_field_pred_argmax and resource_field_pred_argmax_encoder_path on BOTH arms, so the OFF arm is the untrained within-run chance baseline rather than a null column. | PRE-REGISTERED NULL: if ON does not clear the 1.0 D3 floor and the decode did not lift, shape (b) (routing the raw field past z_world as a side-channel) is the next build -- a real pre-registered outcome, not a failure of this run. | Smoke PASS (--dry-run 2026-09-02): both arms constructed, P0a field leg confirmed ACTUALLY RAN on the ON arm only (used_resource_field_head True w=0.5 vs False w=0.0), encoder-path PR 3.74 vs sense-time 5.07 confirming the two paths genuinely differ, F5c precondition correctly marked the ON arm red under a 2-episode budget WITHOUT vacating the OFF arm, and per-claim direction correctly read 'unknown' rather than weakening INV-088. validate_experiments.py --strict OK on all 30 checks. Contract gate: 5525 passed full six-root suite (hub) plus 819 passed on the corpus-lint + SD-018 subset. | episodes_per_run 1350 = 60 P0a + 200 P0 + 90 P1-REINFORCE + 1000 P1-PPO per TRAINING cell; the 2 anchor conditions train nothing. | FISHTANK VIEWER: REE_assembly 4c14694f13 adds the SD-018 overlay that draws the true gradient peak and both predicted peaks on the tank, with a majority-cell baseline (NOT uniform 1/25) because an untrained constant-argmax head reads 95% against uniform chance. | RE-DERIVE BRAKE (step 2.5b): FIRES on both claims (INV-088 4 counted autopsies, MECH-457 11) and is ADJUDICATED EXEMPT under the documented 'redesign of a different mechanism' clause -- stated explicitly rather than ignored. Those autopsies established a ceiling against the PRE-AMEND substrate; this run is the first test of the SD-018 directional resource-field head, which did not exist before ree-v3 028a625 (2026-09-02) and could not receive a single gradient step from any driver in this family until the P0a seam landed (65c37c8, same day). It is therefore not a same-granularity lettered re-test that re-derives a known ceiling -- the substrate under it changed. Both claims independently carry pending_retest_after_substrate: True, i.e. they are flagged as AWAITING exactly this re-test once the blocking substrate lands, and it has. Two notes on the brake text itself, recorded so a later reader does not have to re-derive them: (a) it names 'SD-e1-rollout-consistency-training' as INV-088's blocking upstream, taken from the latest counted autopsy (V3-EXQ-954, 2026-08-29) -- a DIFFERENT mechanism from the one this run tests; (b) it reports that substrate as not IMPLEMENTED, but ree-v3/CLAUDE.md line 18943 records SD-e1-rollout-consistency-training ITEM 2 as IMPLEMENTED 2026-09-01 (validated by V3-EXQ-976), so the brake appears to be matching an older ITEM's status under the same substrate name.",
      "status": "claimed",
      "claimed_by": {
        "machine": "ree-cloud-3",
        "claimed_at": "2026-09-02T17:50:54Z"
      },
      "claim_ids": [
        "INV-088",
        "MECH-457"
      ],
      "conditions": 4,
      "episodes_per_run": 1350,
      "experiment_type": "v3_exq_978_sd018_directional_field_fishtank",
      "seeds": 3
    },
    {
      "queue_id": "V3-EXQ-969",
      "title": "ContextMemory write-content discrimination portfolio, leg H2 (operating-point): does any weight in {0.1,0.5,2.0,8.0} escape 956's wrong-direction result?",
      "script": "experiments/v3_exq_969_contextmemory_write_content_h2_operating_point.py",
      "priority": 65,
      "machine_affinity": "any",
      "estimated_minutes": 480,
      "note": "Leg H2 (operating-point) of the frozen contextmemory_write_content_discrimination portfolio (H1-H4, chip-20260830-ctxmem-write-content-h1h4-portfolio), registered from the V3-EXQ-956 autopsy after V3-EXQ-436g validated write-address OCCUPANCY (16/16 slots, 5/5 seeds) while leaving write CONTENT unaddressed (NO_WRITES calibration arm: mean pairwise slot cosine 0.000 vs 0.999+ for written arms). H2's own null: w=0.5 (956's only tested operating point) is the wrong weight; NO weight in {0.1,0.5,2.0,8.0} yields content discrimination in the correct direction at Bonferroni-corrected significance. DESIGN: Phase A (10 new seeds at w=0.5, paired sign-flip permutation test replicating/testing 956's own wrong-direction finding) + Phase B (weight sweep {0.1,2.0,8.0}, 6 seeds each, unpaired exact permutation test vs Phase A's untrained pool), 38 total cells (20+18). LOAD-BEARING: PASS iff >=1 of 4 tested configs clears BOTH the 0.25 Jaccard-drop margin AND a Bonferroni-corrected (alpha=0.0125 across 4 configs) exact permutation p-value AND the tagger demonstrably moved from init. GOV-REUSE-1 (step 2.4): 956's own cells were minted WITHOUT include_driver_script_in_hash=False and used a different training_episodes (100 vs this driver's moderate-scale 40), so arm-fingerprint reuse is refused regardless; not recoverable, proceeding fresh. Step 2.5c (re-checked 2026-09-01 against the CURRENT substrate_queue.json): the only open corrupting entries naming ree_core/agent.py at module grain (mode-governance-engagement, SD-082) are removed by construction (this driver never sets use_salience_coordinator=True or use_lateral_pfc_analog=True, both left at REEConfig's own False default); contextmemory-write-path-addressing-degeneracy (e1_deep.py::ContextMemory.write) IS the entry this leg targets, not an unrelated overlap -- the sanctioned exception per this portfolio's own chip brief. Step 2.5b: N/A, claim_ids=[]. red-team (fable), 2026-09-02: CONTESTED -> 2 findings. G1 (criterion cannot discriminate by construction at moderate baseline) -- DISMISSED with a disclosed caveat: the Bonferroni-corrected permutation test is arithmetically unreachable for a baseline mean_jaccard_untrained roughly in [0.25,0.65), which contains 956's own measured baseline (0.4); NOT fixed here (would need a materially larger seed/compute budget, out of this pass's remit, and this is leg 1 of 4) -- H2_per_config_table always records observed_diff and p_value per config regardless of outcome, so a bare FAIL can be manually checked against the attainable zone before being read as informative. G2 (a gate certified its own subject) -- FIXED: criteria_non_degenerate's tagger-moved check (winning_config_tagger_moved) was vacuously True on every FAIL (no winning config exists to invalidate), so a silent wiring failure (tagger never receiving gradient in ANY trained cell) could have presented as a valid null; now also requires the aggregate positive control (n_trained_moved == len(trained_rows)). Smoke PASS (--dry-run, 2026-09-02, re-verified after fix): all 5 dry-run cells printed verdict lines, ep N/M present, z_goal_stream unmeasured (nothing to wire; ticks=0/0 at this toy scale). COST: 38 cells x 40 episodes x 150 steps; estimated_minutes is a rough extrapolation from the 956/943 lineage's own historically-recorded 100ep/5seed/3cond range (150-900 min for that driver family across different estimation passes), scaled by cell count and episode budget -- approximate, not measured on this exact driver.",
      "status": "pending",
      "claim_ids": [],
      "conditions": 38,
      "episodes_per_run": 40,
      "experiment_type": "v3_exq_969_contextmemory_write_content_h2_operating_point",
      "seeds": 1
    },
    {
      "queue_id": "V3-EXQ-970",
      "title": "ContextMemory write-content discrimination portfolio, leg H1 (loss-objective-mismatch): does a content-referencing contrastive loss achieve content-conditioned write addressing?",
      "script": "experiments/v3_exq_970_contextmemory_write_content_h1_contrastive_loss.py",
      "priority": 62,
      "machine_affinity": "any",
      "estimated_minutes": 420,
      "note": "Leg H1 (loss-objective-mismatch) of the frozen contextmemory_write_content_discrimination portfolio (H1-H4, chip-20260830-ctxmem-write-content-h1h4-portfolio). H1's null: the existing pairwise-diversity write-addressing loss references no content label; swapping in a content-referencing (contrastive) objective at the same schedule does not drop Jaccard by the 0.25 margin in EITHER of two regimes. DUAL REGIME (avoids conflation with sibling H4, V3-EXQ-972): Regime A (real agent, env-driven safe/dangerous context, held-out real-latent probe, 100ep x 150 steps) and Regime B (synthetic 2-cluster stream, direct 15000-step SGD on the tagger alone, matching V3-EXQ-907's own real schedule). LOAD-BEARING: PASS iff EITHER regime's own readiness gate is green AND its Jaccard drops by >=0.25 with a Bonferroni-corrected (alpha=0.025 across 2 regimes) paired sign-flip permutation p-value -- OR-combination is deliberate (either regime succeeding is evidence FOR H1; the null requires BOTH to fail). GOV-REUSE-1 (step 2.4): the decisive readout (Jaccard for a content-REFERENCING objective) is not recorded anywhere -- 956 trained under a different, content-BLIND loss and was minted without include_driver_script_in_hash=False regardless; not recoverable, proceeding fresh. Step 2.5c (re-checked 2026-09-01): same disposition as 969/972 -- only the entry under test (contextmemory-write-path-addressing-degeneracy) overlaps; mode-governance-engagement/SD-082 removed by construction. Step 2.5b: N/A, claim_ids=[]. red-team (fable), 2026-09-02: BLOCKING -> FIXED, then re-verified CLEAR for the fix. Original finding: the load-bearing exact sign-flip permutation test's minimum ATTAINABLE one-sided p-value at n=5 seeds is 1/2**5=0.03125, exceeding ALPHA_CORRECTED=0.025 -- the PASS branch was unreachable under EVERY possible outcome, including a perfect same-direction result on all 5 seeds, in BOTH regimes (they share SEEDS). FIXED by extending SEEDS from 5 to 6 seeds ([42,7,13,100,200,300]): 1/2**6=0.015625 < 0.025 restores attainability. Independent re-verification pass (fable, same day) confirmed CLEAR: all 6 seeds genuinely flow through both regimes' load-bearing computation (no stale 5-length slice or hardcoded list found), the arithmetic holds against the script's own current ALPHA_CORRECTED, and the added seed is a linear compute-cost increase only, no correctness issue. An earlier design pass (documented in-file as Findings F1-F4, 2026-09-01) had already fixed: F1 (Regime B's probe tested generalization to unseen clusters, not training success), F2 (Regime A's probe was on unrelated synthetic clusters rather than genuinely held-out real latents), F3 (a 158x training-update-budget asymmetry between regimes, reduced to ~10x), F4 (a bare mean-margin test's ~15% false-positive rate under the null, replaced with the Bonferroni-corrected permutation test that F5/this pass then found unreachable at n=5 and fixed by raising n to 6). Smoke PASS (--dry-run, 2026-09-02, re-verified after fix): Regime A cells FAIL at toy scale (expected -- 2 episodes cannot build a real state buffer), Regime B cells PASS; ep N/M and verdict lines present; z_goal_stream unmeasured. COST: Regime A 12 cells x 100ep x 150 steps + Regime B 12 cells (6 trained @ 15000 SGD steps on a small MLP, 6 untrained/eval-only); estimated_minutes is a rough extrapolation from the 956/943 lineage's own recorded range, approximate.",
      "status": "pending",
      "claim_ids": [],
      "conditions": 4,
      "episodes_per_run": 100,
      "experiment_type": "v3_exq_970_contextmemory_write_content_h1_contrastive_loss",
      "seeds": 6
    },
    {
      "queue_id": "V3-EXQ-971",
      "title": "ContextMemory write-content discrimination portfolio, leg H3 (task-pressure-required): does routing the read-path's task gradient through write selection achieve content conditioning?",
      "script": "experiments/v3_exq_971_contextmemory_write_content_h3_task_coupled.py",
      "priority": 60,
      "machine_affinity": "any",
      "estimated_minutes": 300,
      "note": "Leg H3 (task-pressure-required) of the frozen contextmemory_write_content_discrimination portfolio (H1-H4, chip-20260830-ctxmem-write-content-h1h4-portfolio). H3's null: no standalone write-side auxiliary loss suffices (956's content-blind pairwise-diversity loss and sibling H1's content-referencing standalone loss are both members of this family); coupled training via the READ path's own task gradient still fails the 0.25 Jaccard margin. MECHANISM: a straight-through soft-read (softmax over write_addr_tagger's scores @ frozen memory content) feeds agent.e3.harm_eval_head via a newly-constructed bridging linear layer (read_bridge, memory_dim=128 -> world_dim=32; no existing substrate component has that exact shape), producing a real harm-prediction loss against the environment's own harm label. Two arms: H3_UNTRAINED (loss computed for logging only via a detached forward pass, weight=0.0, structurally never added to the backward graph) vs H3_TRAINED (weight=1.0, genuinely added to total before optimizer.step()). Optimizer group excludes harm_eval_head's own parameters (a separate harm_eval_opt zero_grads its stale gradient before its own unrelated step). LOAD-BEARING: PASS iff the untrained-baseline readiness precondition (>=0.25) holds AND mean_jaccard(TRAINED) <= mean_jaccard(UNTRAINED) - 0.25. GOV-REUSE-1 (step 2.4): the decisive readout (task-COUPLED gumbel_learned Jaccard) is not recorded anywhere; not recoverable, proceeding fresh. Step 2.5c (re-checked 2026-09-01): same disposition as 969/970/972. Step 2.5b: N/A, claim_ids=[]. red-team (fable), 2026-09-02: CONTESTED -> 3 findings. G1 (manipulation could not reach the DV at full strength) -- FIXED: agent.e3.harm_eval_head already ends in its own nn.Sigmoid() (harm in [0,1], per e3_selector.py), so feeding its output through F.binary_cross_entropy_with_logits (both in this driver's own h3_loss AND in the pre-existing harm_eval_opt loop inherited from V3-EXQ-956) was a double sigmoid: the effective probability is confined to sigmoid((0,1))=(0.5,0.731) with no attainable stationary point, so the loss perpetually saturates and its gradient into read_bridge/soft_probs/write_addr_tagger collapses. Harmless scaffolding in 956; load-bearing here, since it is H3's own gradient-transmission line. Fixed at both call sites to plain F.binary_cross_entropy on the head's already-probabilistic output. G2 (verdict grid over-claims on FAIL) -- DISMISSED with a disclosed caveat: a FAIL does not distinguish 'H3 is false' from 'read_bridge (a full trainable layer in the same optimizer group) found a shortcut that reduces h3_loss without meaningfully engaging the tagger' or 'frozen memory content carries no harm-relevant information to route regardless of H3's truth'; NOT fixed here (isolating this needs a third, detached-scores control arm -- a bigger design change than this pass's remit). Only the FAIL direction is affected: a PASS remains fully attributable to H3, since read_bridge alone has no mechanism to move a downstream synthetic-probe Jaccard it never touches. G3 (gate certifies its own subject, noted, non-blocking): the mechanical positive control's threshold (TAGGER_MOVE_EPS=1e-8) is loose relative to Adam's own step size and certifies graph connectivity rather than usable signal -- true but non-gating/descriptive only; the paired negative control (Gumbel noise cannot move params, sampling lives inside write()'s own no_grad block) is sound. Smoke PASS (--dry-run, 2026-09-02, re-verified after fix): both arms printed verdict lines, ep N/M present, z_goal_stream unmeasured. COST: 10 cells x 100 episodes x 150 steps; estimated_minutes approximate, extrapolated from the 956/943 lineage's own recorded range.",
      "status": "pending",
      "claim_ids": [],
      "conditions": 2,
      "episodes_per_run": 100,
      "experiment_type": "v3_exq_971_contextmemory_write_content_h3_task_coupled",
      "seeds": 5
    },
    {
      "queue_id": "V3-EXQ-972",
      "title": "ContextMemory write-content discrimination portfolio, leg H4 (input-distribution): is the real train-time write-stream representation separable by context at all?",
      "script": "experiments/v3_exq_972_contextmemory_write_content_h4_input_distribution.py",
      "priority": 58,
      "machine_affinity": "any",
      "estimated_minutes": 150,
      "note": "Leg H4 (input-distribution) of the frozen contextmemory_write_content_discrimination portfolio (H1-H4, chip-20260830-ctxmem-write-content-h1h4-portfolio). H4's null (this leg's own falsifier, verbatim from the frozen registry): 'latents carry separable structure the tagger ignores' -- i.e. if the actual write-stream states already show clear inter-context separation via a simple representation-only statistic, that REFUTES H4 and refocuses blame on H1/H3 (the addressing ALGORITHM, not the input it receives). MEASUREMENT-ONLY diagnostic, single arm (GUMBEL_TRAINED, identical training config to V3-EXQ-956's own trained arm -- this leg does not test whether addressing training succeeds, it instruments the INPUT that training receives regardless of outcome): instruments the real write-stream state tensor at every step, bucketed by safe/dangerous context, records intra_safe_cosine / intra_dangerous_cosine / inter_cosine / separability_score = mean(intra) - inter. LOAD-BEARING criterion (H4_representation_separability_measured) is a MEASUREMENT-VALIDITY gate only (passes iff >=3/5 seeds had enough buffered samples, >=50 per bucket, regardless of the sign of separability_score) -- the actual scientific reading (interpretation.h4_reading: 'representation_separable' if mean separability_score > 0.05, else 'representation_undifferentiated') is explicitly non-gating and descriptive. GOV-REUSE-1 (step 2.4): 956 never recorded per-step write-stream state tensors (only deterministic occupancy DVs and the post-training synthetic-probe Jaccard), and was minted without include_driver_script_in_hash=False regardless; not recoverable, proceeding fresh. Step 2.5c (re-checked 2026-09-01): same disposition as 969/970/971 -- contextmemory-write-path-addressing-degeneracy IS the entry this leg's write-stream is drawn from; mode-governance-engagement/SD-082/SD-e1-rollout-consistency-training all removed by construction (this driver never enables their gating flags or code paths). Step 2.5b: N/A, claim_ids=[]. red-team (fable), 2026-09-02: CONTESTED -> 1 finding, DISMISSED with a disclosed caveat rather than fixed. separability_score has no null control for temporal/episode autocorrelation: intra-bucket pairs include same-episode and same-5-episode-block pairs while inter-bucket pairs are always cross-block and time-separated, so slow representation drift over 100 episodes could produce intra>inter with zero content-CONDITIONED structure; only scalar stats reach the manifest (no per-state episode/step index), so no permutation/shuffle null can be computed post-hoc if this is ever suspected. NOT fixed here (the concrete fix -- store (episode_idx, step_idx) per buffered state, add a block-shuffle permutation control -- is a real but non-trivial instrumentation addition, out of this pass's scope). Mitigating factors recorded in the script docstring: the load-bearing criterion already gates only on sample-sufficiency, not on the sign of separability_score; intra_safe/intra_dangerous cosines are reported separately so a reader can see a uniformly-tight cone (autocorrelation-consistent); and the non-gating cross-reference against 956's own synthetic-probe Jaccard (not subject to this confound) gives an independent check -- persistent disagreement between the two readings would flag a governance reviewer to demand the permutation-null treatment before trusting a 'representation_separable' reading at face value. Smoke test at toy scale correctly self-routes to a readiness FAIL (2 episodes cannot reach the 50-sample-per-bucket floor) -- expected, not a bug; verdict line and ep N/M present, z_goal_stream unmeasured. COST: 5 cells x 100 episodes x 150 steps (single arm); estimated_minutes approximate, extrapolated from the 956/943 lineage's own recorded range.",
      "status": "pending",
      "claim_ids": [],
      "conditions": 1,
      "episodes_per_run": 100,
      "experiment_type": "v3_exq_972_contextmemory_write_content_h4_input_distribution",
      "seeds": 5
    }
  ]
}
