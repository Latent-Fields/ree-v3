{
  "schema_version": "v1",
  "calibration": {
    "ms_per_episode_condition": 8000,
    "hardware": "Apple M2 Air 8 GB",
    "calibrated_at": "2026-03-16",
    "calibration_note": "With hazard_harm=0.02, avg episode ~182 steps (was ~18 with 0.5). Actual episode time higher."
  },
  "items": [
    {
      "queue_id": "V3-EXQ-996",
      "title": "GAP-14 successor: LIVE phase-gate validation via env_kwargs() (crossing-count vs legacy spike), with a raw-trajectory-divergence precondition -- 591h vacuity fix",
      "script": "experiments/v3_exq_996_isef005_phase_gate_live_channels.py",
      "priority": 45,
      "machine_affinity": "any",
      "estimated_minutes": 360,
      "supersedes": "V3-EXQ-591h",
      "claim_id": "",
      "backlog_id": "EVB-1189",
      "note": "REDESIGN of V3-EXQ-591h (NEW EXQ number, not a 591 letter, per infant_gap14_redesign_staged_20260827.md and the autopsy's own instruction (c)). Routed by the user-adjudicated failure_autopsy_V3-EXQ-591h_2026-09-03 (ratified REE_assembly d51fc6aaa5, recorded on infant_substrate:GAP-14 governance_2026_09_03): 591h PASSED 5/5 seeds but was VACUOUS -- per_episode_h_pos was BIT-IDENTICAL between ARM_SPIKE and ARM_CROSSING on 5/5 seeds because the only phase->agent channel the 591b-591h lineage wired was config.e3.novelty_bonus_weight, whose consuming branch was DELETED 2026-05-25 (dead-by-construction).\n\nORIGINAL GOVERNANCE INSTRUCTION named TWO required live channels: env_kwargs() and config_overrides()['offline_integration_frequency']. THIS DESIGN SHIPS ONLY ONE (env_kwargs()) -- the second was investigated, wired in a first draft, then DROPPED after the mandatory Step 4.5 adversarial red-team (run on a different model, fable) found the workaround for it was itself a dead channel. Full disposition below.\n\n(a) env_kwargs() -- LIVE, SHIPPED. InfantCurriculumScheduler.env_kwargs() is spread in full into the CausalGridWorldV2 constructor every episode (harm_gradient_enabled -> causal_grid_world.py:2617 harm-gradient reward term consumed by update_residue() -> ResidueField.accumulate() -> ARC-046 hazard-avoidance term in E3 trajectory scoring; transient_benefit_enabled -> :3077 stochastic benefit-patch spawn). Genuine closed loop, verified reachable by direct construction probe (Step 2.5a) before any script code was written, AND verified firing live in the final script via a diagnostic tick counter (info['transition_type']=='harm_gradient'; see n_harm_gradient_ticks / n_harm_gradient_ticks_by_cell in the manifest) -- added specifically per red-team finding F3 to directly confirm the channel engages, not merely that it is wired.\n\n(b) offline_integration_frequency -- INVESTIGATED, NOT SHIPPED (RED-TEAM F1). config_overrides()['offline_integration_frequency'] (10->20 on Phase0->1) matches agent.py:12070's should_integrate() cadence field, but REEAgent.offline_integration() itself is confirmed-broken (a bare torch.cat([s,w]) at default dim=0 instead of dim=-1, agent.py ~12053-12058, crashing once the E1 experience buffer exceeds 10 entries -- confirmed via a direct 15-step probe and by comparison to the correct sibling usage at agent.py:5775-5777; this is why grep confirms zero scripts in the whole ree-v3/experiments/ corpus have ever called agent.offline_integration() or agent.should_integrate()). A FIRST DRAFT worked around this by calling should_integrate() at the correct cadence and, on a True tick, invoking only the confirmed-safe residue-only half, agent.residue_field.integrate(). Step 4.5 red-team (fable) found THIS HALF IS ITSELF DEAD: ResidueField.integrate() (ree_core/residue/field.py:1120-1141) computes F.mse_loss(neural_field(...), rbf_field(...)).item() with NO .backward(), NO optimizer, NO .step(), and NO in-place write anywhere -- neural_field's parameters are referenced nowhere else in the whole ree_core/ tree, so nothing anywhere trains them. Its only real effect is that torch.randn_like(...) (10 draws/call) advances the GLOBAL torch RNG stream, which every downstream stochastic draw (CEM noise, E3 multinomial, replay) then consumes differently on a schedule correlated with curriculum phase -- a genuine but SPURIOUS source of per_episode_h_pos divergence between arms that the new P6 precondition (below) CANNOT distinguish from a real one. Wiring it as originally drafted would have manufactured a subtler repeat of the exact 591h vacuity failure rather than fixing it. DECISION: this channel is not wired at all in the shipped script; agent.config.offline_integration_frequency is never set; should_integrate()/residue_field.integrate() are never called. Recorded as TWO entries in the manifest's substrate_defect_found (one per confirmed-dead half) and folded into scope_limitation. config.e3.novelty_bonus_weight remains deliberately unwritten too (dead since 2026-05-25, per the original instruction).\n\nNEW LOAD-BEARING PRECONDITION (governance requirement (b), independent of the channel-count question above): raw_trajectory_divergence_present, evaluated FIRST (before gate_green/dropout_bypassed/c_discriminates) -- counts episodes across ALL seeds where ARM_SPIKE and ARM_CROSSING's RAW per_episode_h_pos sequences differ by more than float noise (|diff|>1e-9). This is the exact guard 591h lacked: its own C_arms_differ compared the gate's DECISION output (reached_phase1, phase_01_at), which can (and did) report True even while behaviour was bit-identical, because a different threshold applied to an IDENTICAL underlying signal still fires at a different episode. Below floor -> label=substrate_not_ready_requeue / vacuous, never a substrate-verdict label. C_arms_differ_decision_level is retained as a diagnostic-only (non-load-bearing) criterion, explicitly named in the script docstring as 'the 591h-insufficient statistic'. New EXQ number, claim-free (CLAIM_IDS=[]; 591h's ARC-019 tag is not carried forward, per the autopsy's tag-removal instruction).\n\nRED-TEAM (Step 4.5, fable, independent model from the author) -- SECOND FINDING (F2), ALSO FIXED: once env_kwargs() is genuinely live, ARM_SPIKE's own post-advance episodes run under different (Phase-1) dynamics that depend on ARM_SPIKE's own gate decision -- so the ORACLE (h_pos_mean_full_run on ARM_SPIKE, used as ground truth for 'is this seed a genuine explorer') and P2 (a post-hoc recount of ARM_SPIKE's post-advance h_pos values against the spike threshold) both became contaminated by the manipulation under test, unlike in 591h where env_kwargs was informational-only. FIXED: (i) oracle switched to h_pos_mean_pre_ep_min -- the mean over episodes [0, PHASE_EP_MIN[1]=100), provably Phase-0-only and bit-identical-in-composition across arms since InfantCurriculumScheduler._try_phase_0_to_1 refuses to advance before episode 100 under either gate rule; the ORACLE_H_POS_MEAN_FLOOR=0.20 numeral was originally calibrated against 591d/591f's full-run means, so applying it to the pre-ep_min statistic is an approximation -- readiness preconditions P3/P4 self-route to substrate_not_ready_requeue rather than silently passing through a poorly-calibrated floor. (ii) P2 (crossing_counts_reach_gate_minimum) switched to read ARM_CROSSING's own live scheduler_crossing_count directly (the exact statistic the gate itself computes and routes on) instead of a post-hoc recount off a different arm's trajectory.\n\nRED-TEAM MINOR/NOTE FINDINGS (no further script change beyond F3's cheap sharpening, described under channel (a) above): F4 confirmed the torch.cat bug claim is real (independently traced); F5 verified no other vacuity risk (arm_cell resets all RNG per seed so Phase-0 trajectories are cross-arm bit-identical by construction; replay_diversity_enabled defaults False so hippocampal stdlib-random sites are unreachable; residue_scale_factor scope note accurate; env-kwarg spread, should_integrate() arithmetic, scheduler wiring, and phase-timing bounds all check out); F6 (cosmetic) corrected a pre-filled 'red-team: CLEAR' docstring line that predated the actual review.\n\nSCOPE LIMITATIONS (deliberate, documented in the script docstring and manifest): config.e3.novelty_bonus_weight NOT written (dead channel). residue_scale_factor NOT applied (governance named only env_kwargs()+offline_integration_frequency as required channels; out of scope to add a third). offline_integration_frequency NOT applied at all (see RED-TEAM F1 above -- both its E1-replay half and its residue-only half are confirmed dead). No torch.multinomial-dependent assertions on committed/sampled actions -- all thresholds are on upstream continuous quantities (h_pos, crossing counts), for cross-machine-class safety.\n\nGOV-REUSE-1 (2.4): decisive readout n_diverging_episodes_total / C_live_gate_discriminates. Checked evidence/experiments/ and the queue/experiments tree for any existing run wiring env_kwargs() live on this curriculum -- none found (only the 591 lineage exists, all confirmed wiring only novelty_bonus_weight). Not recoverable from existing data -> run.\n\nSTEP 2.5c (substrate-path overlap gate): queried evidence/planning/substrate_queue.json for open 'corrupting' entries touching causal_grid_world.py/agent.py/infant_curriculum.py. Two found (mode-governance-engagement, SD-082) -- both gated behind flags (use_salience_coordinator/use_external_task_drive, use_lateral_pfc_analog) this script's diversity-armed build does not enable (verified against _agent_config_kwargs()). Ruled non-blocking.\n\nCOUPLED CONTRACT PIN CHECKED (ree-v3/tests/contracts/test_infant_curriculum_gap9.py:262-264, test_c7_config_overrides): read in full -- it asserts only properties of InfantCurriculumScheduler.config_overrides()'s own return value (residue_scale_factor increasing, offline_integration_frequency increasing 10->20, novelty_bonus_weight>0 in every phase), never references any 591-family driver. This script does not modify config_overrides()'s shape, only which fields (if any) the driver consumes. NO CHANGE NEEDED; no working-tree changes were made to this file or any other tracked file (the new driver script is the only untracked addition).\n\nDESIGN (unchanged from 591f/591h): discriminative pair, ARM_SPIKE (legacy single-episode spike gate) vs ARM_CROSSING (phase_0to1_use_crossing_count=True, min 3 crossings per 591f). Seeds 42-46, 160 episodes, 200 steps/episode, diversity-armed agent (MECH-313 noise floor + MECH-314 structured curiosity, SP-CEM main-path default). REFERENCE_591F frozen positive-control cells (verified byte-identical to 591h's own values) used via assert_anchor_reachable at setup to structurally prove every readiness predicate can clear its own gate -- unaffected by the pre-ep_min oracle switch since it is a structural check on the predicate machinery only, not a live-run comparison. Readiness preconditions P1/P3/P4/P5 carried over unchanged (spike-arm-reproduces-advance>=2, oracle-genuine-explorers-present>=1, oracle-non-explorers-present>=1, live-false-advancer-present>=1); P2 redefined per red-team F2 above. LOAD-BEARING C_live_gate_discriminates: in ARM_CROSSING, for every seed, the live phase-advance decision agrees with the ORACLE label -- gated behind the new P6 divergence precondition.\n\nvalidate_experiments.py --strict: clean pass, 0 warnings across all 30 checks, no exemptions (re-verified after the F1/F2 fixes). --dry-run smoke (2 seeds x 2 arms, 3 episodes x 25 steps): exit 0 both before and after the F1/F2 fixes, all 4 cells ran to completion with real env_kwargs applied per phase (confirmed via [train] log lines printing the live env_kwargs dict each episode), z_goal_stream smoke 'no writer defect', correctly self-routed label=substrate_not_ready_requeue (expected at toy scale -- no seed reaches the spike gate in 3 episodes, so precondition P1 correctly fails closed rather than asserting a false substrate verdict).\n\nCOST: estimated_minutes set to 360, matching 591h's OWN measured wall-clock (5h46m for the same 2-arm x 5-seed x 160-episode x 200-step design on ree-cloud-2, per that queue entry's own COST note) -- this script's overhead beyond 591h's (env_kwargs() spread, one diagnostic tick counter) is negligible relative to per-step CEM planning cost, and it does LESS work than a first-draft two-channel version would have (no should_integrate()/residue_field.integrate() calls at all), so the per-step cost profile is at or below 591h's.\n\nQUEUE-APPEND SERIALIZATION: this entry is prepared by a queue-experiment subagent operating under a centrally-serialized fan-out (a /failure-autopsy pause lock currently covers ree-v3/experiment_queue.json, REE_assembly/evidence/, claims.yaml, WORKSPACE_STATE.md). Per the delegating orchestrator's explicit instruction, this session does NOT edit experiment_queue.json, does NOT commit, does NOT push, and does NOT run /session-land -- the orchestrator lands this entry (and the coupled-set: this entry alone, since the contract-pin check above concluded no contract change is needed).",
      "status": "claimed",
      "claimed_by": {
        "machine": "ree-cloud-3",
        "claimed_at": "2026-09-03T20:37:55Z"
      },
      "claim_ids": [],
      "conditions": 2,
      "episodes_per_run": 160,
      "experiment_type": "v3_exq_996_isef005_phase_gate_live_channels",
      "seeds": 5
    },
    {
      "queue_id": "V3-EXQ-997",
      "title": "MECH-162 z_resource/z_world hippocampal-planning reconvergence (ghost-goal retrieval discrimination)",
      "script": "experiments/v3_exq_997_mech162_zresource_zworld_planning_reconvergence.py",
      "priority": 40,
      "machine_affinity": "any",
      "estimated_minutes": 20,
      "claim_id": "MECH-162",
      "backlog_id": "EVB-1447",
      "note": "Step 2.4 GOV-REUSE-1: grep confirmed zero prior manifests/autopsies/scripts reference MECH-162 (checked 2026-09-03) -- not recoverable, ran fresh. Step 2.5b re-derive brake: 0 counting autopsies -- not braked. Step 2.5c substrate-path overlap: two OPEN substrate_queue entries file-match ree_core/agent.py (mode-governance-engagement, SD-082) but neither exercises this driver's config surface -- does not block. Design: 2-arm x 5-seed retrieval-discrimination probe over the MECH-292/293 ghost-goal-bank pathway (current_z_goal -> propose_trajectories -> MECH-293 ghost probes -> MECH-292 GhostGoalBank.rank() -> Anchor.goal_match, SD-079-centered cosine). ARM_ZRESOURCE seeds z_goal from the SD-015 object-identity latent at genuine contact; ARM_ZWORLD from the legacy full-scene z_world latent. DV: per-tick discrimination_delta = mean(SAME_TYPE goal_match) - mean(DIFF_TYPE goal_match) over anchors written within the same episode (agent.reset() wipes the whole SD-039 anchor set at every episode boundary -- confirmed by source read -- so cross-episode accumulation is architecturally impossible). Step 4.5 red-team (different model, fable) found and I fixed THREE substrate-invalidating defects before any run: (1) obs_dict['resource_type_at_agent'] reads 0 on every consumption tick under the default legacy auto-consume-on-entry env mode -- contact detection now deferred one tick, sourced from info['sd049_consumed_type_tag_this_tick'] (V3-EXQ-681 convention). (2) write_anchor's dual-trace demotion remaps a demoted anchor's goal_payload onto whatever new contact caused its demotion, corrupting cross-type comparisons under a shared anchor family -- fixed by partitioning anchor families per resource type AND per write (unique per contact), so demotion never remaps across (or even within) types. (3) GhostGoalBankConfig defaults to include_inactive=True/include_active=False -- combined with fix (2)'s unique-per-write families nothing is ever demoted, so the default pool would stay empty -- fixed by include_active=True on both arms. Also enabled SD-079 goal_cue_centering=True (raw uncentered cosine sits in a near-degenerate ~0.985-1.000 band, compressing any real signal below the effect floor). READINESS CALIBRATION (orchestrator-directed re-issue after the fix pass): v1 schedule (NUM_RESOURCES=4/type -> 8 total on 10x10 grid, N_EPISODES=20, STEPS_PER_EPISODE=80) was internally verified (5-seed x 2-arm run against the fully fixed substrate) and measured READINESS NOT MET on either arm (ARM_ZRESOURCE R1=0.6/0.8 R2=0.4/0.8; ARM_ZWORLD R1=0.8/0.8 R2=0.4/0.8 -- one seed, 43, got zero contacts across the full 1600-step cell) -- outcome FAIL/non_contributory/substrate_not_ready_requeue, C1 also read negative and noisy (mean delta -0.036, seed_fraction 0.2/5). Density and schedule were then raised (NUM_RESOURCES 4->10/type -> 20 total, 20% grid density; N_EPISODES 20->25; STEPS_PER_EPISODE 80->150 -- DV, arms, thresholds and the three red-team fixes left untouched) and re-verified (v2, this schedule): READINESS NOW MET on BOTH arms at 0.8 seed-fraction (4/5, exceeding the 0.8 threshold and a clear majority) -- ARM_ZRESOURCE R1=0.8/0.8 R2=0.8/0.8; ARM_ZWORLD R1=0.8/0.8 R2=0.8/0.8. Seed 43 remains a persistent near-zero-contact outlier in BOTH v1 and v2 (1 contact v2 ARM_ZRESOURCE, 0 elsewhere) -- a genuinely degenerate untrained-policy trajectory for that seed/env-config combination, not a code defect (4/5 other seeds contact abundantly, 13-126 contacts/cell at v2 density). --dry-run smoke at v2 density now shows genuine natural activity even at the short 3x10-step smoke scale (active_frac=0.900, writer_calls=8, 'no writer defect'), corroborating the fix is robust, not a lucky full-run artifact. RESULT AT v2 (this queued schedule, internal verification, NOT the official run): outcome FAIL, evidence_direction weakens, label mech162_zresource_no_discrimination_advantage_over_zworld. Both arms show a genuine POSITIVE within-episode SAME_TYPE-over-DIFF_TYPE discrimination signal (ARM_ZRESOURCE per-qualifying-seed discrim 0.196/0.121/0.181/0.303; ARM_ZWORLD 0.073/0.132/0.256/0.021 on seeds 42/45/46/47) -- the mean cross-arm delta (0.064) clears EFFECT_FLOOR (0.02) -- but ARM_ZRESOURCE's advantage over ARM_ZWORLD is NOT consistent across the required seed-majority (ZRESOURCE>ZWORLD on only 2/4 qualifying seeds -- 42 and 47 -- vs 45 and 46 where ZWORLD is higher; seed_fraction 0.4 vs 0.6 threshold). This is a genuine, informative null on the specific pre-registered identity-invariant-to-position prediction: the MECH-292/293 ghost-retrieval channel discriminates same-vs-different-type goal cues from BOTH z_resource and z_world seeding, not preferentially from z_resource as MECH-162's notes predicted -- it does not by itself rule out convergence, but does not support the specific representational-advantage claim either. Recording standard: write_flat_manifest via pack_writer, per-cell arm_cell fingerprinting, emit_outcome sentinel, z_goal_stream_stats via ZGoalStreamAccumulator, ASCII-only prints, claim_ids=['MECH-162'] only (MECH-236's channel-existence wiring is a precondition reused, not re-tested).",
      "status": "pending",
      "architecture_epoch": "ree_hybrid_guardrails_v1",
      "claim_ids": [
        "MECH-162"
      ],
      "conditions": 2,
      "episodes_per_run": 25,
      "experiment_type": "v3_exq_997_mech162_zresource_zworld_planning_reconvergence",
      "seeds": 5
    }
  ]
}
