{
  "schema_version": "v1",
  "calibration": {
    "ms_per_episode_condition": 8000,
    "hardware": "Apple M2 Air 8 GB",
    "calibrated_at": "2026-03-16",
    "calibration_note": "With hazard_harm=0.02, avg episode ~182 steps (was ~18 with 0.5). Actual episode time higher."
  },
  "items": [
    {
      "queue_id": "V3-EXQ-1057",
      "title": "MECH-017 additive-budget replay: is the recency cost intrinsic to replay, or an artefact of reallocating a fixed gradient budget?",
      "experiment_type": "v3_exq_1057_mech017_reality_consolidation_replay_additive_budget",
      "script": "experiments/v3_exq_1057_mech017_reality_consolidation_replay_additive_budget.py",
      "priority": 8,
      "machine_affinity": "any",
      "status": "pending",
      "estimated_minutes": 22,
      "episodes_per_run": 18,
      "seeds": 5,
      "conditions": 4,
      "claim_id": "MECH-017",
      "note": "MECH-017, the separable successor question V3-EXQ-1048's CONFIRMED autopsy names verbatim (failure_autopsy_V3-EXQ-1048_2026-09-17.json, learning_extracted[0], routing 'queue-experiment', recommended_substrate_queue_entry.action 'none' -- 'the substrate performed as designed; the open question is scientific, not a substrate defect'): is the recency deficit INTRINSIC to replay, or an artefact of REALLOCATING a fixed gradient budget? Under a matched budget every step spent replaying old traces is a step NOT spent on recent ones, so a recency cost is exactly what pure reallocation would produce even if replay is harmless to recency. The two are separable only by ADDING the replay pass to the online budget instead of substituting it. | ARM_D_ADDITIVE_BUDGET is the one named change: at each consolidation point it runs ARM_A's whole-buffer pass AND ARM_B's recent-window pass as two INDEPENDENT sequential consolidator.consolidate(...) calls (2 x CMC_STEPS = A's budget plus B's), never one accumulated backward. _consolidate wraps each call in its own _BufferWindow context manager, so the whole-buffer pass has fully exited (buffers restored) before the recent-window pass narrows them. No new substrate flag and no new ree_core code -- both calls are already-exercised code paths. | CRITERION, NOT INVENTED: load-bearing C4 is V3-EXQ-1048's C2 with ARM_D substituted for ARM_A -- same DV (e1_holdout_mse_late), same comparator arm B, same LATE_TOL = 0.10, same SIGN_CONSISTENCY_REQUIRED = 4. PASS = the deficit was a reallocation artefact; FAIL = it is intrinsic to replay. Re-pointing the late leg at ARM_C is explicitly FORBIDDEN and was NOT done: the autopsy's learning_extracted[1] records that its own first draft did exactly that and was wrong ('the claim itself names arm B on that leg, and arm C only as a readiness control'), and the swap would not even have rescued C2 (3/5 against a threshold of 4). C1/C2/C3 are re-run UNCHANGED but carry load_bearing:false -- replication of 1048 on the same harness. Leaving C2 load-bearing would pin this run to FAIL regardless of what the new question answers, since C2 already failed on 1048. | SCOPE: answers the REALLOCATION question ONLY. The autopsy records that MECH-017 'is two claims wearing one id' ('replay counters forgetting' SUPPORTED, 'at no cost to recency' WEAKENED) and flagged it for a granularity review. This run must NOT be read as adjudicating that bundle; the docstring and the manifest evidence_direction_note both say so. | PRECONDITIONS: all TWELVE of V3-EXQ-1048's kept unchanged and none weakened. gradient_budget_matched still compares A against B ONLY -- ARM_D is deliberately budget-UNMATCHED, which is the manipulation, and that is the single scoped exemption. THREE gates added for the new arm, all guarding it rather than relaxing anything: additive_budget_is_a_plus_b (|D - (A+B)| <= 0.5, so a reader can verify the budget was ADDED not reallocated), additive_manipulation_reaches_dv (D and B must not be bit-identical on the late probes), and additive_arm_retains_replay_gain (red-team F2, below). | Step 2.4 GOV-REUSE-1: decisive readout = e1_holdout_mse_late for an ADDITIVE-budget consolidation arm. NOT recoverable from any recorded manifest -- no prior driver anywhere has an additive-budget arm, so that quantity has never been measured on any substrate. V3-EXQ-1048 is the only MECH-017 run and it carries A/B/C only. Run required. Step 2.5: MECH-017 status=candidate, no v3_pending, no implementation_phase=v3; substrate implemented and the autopsy requests no build. Step 2.5b re-derive brake on MECH-017 = 0 hits (the sole autopsy is recommended_epistemic_category 'standard', not substrate_ceiling). Step 2.5c: the open CORRUPTING entry contextmemory-write-path-addressing-degeneracy is OFF-PATH -- sd016_writepath_mode read off the live config and asserted 'off' in all 20 cells; the only open sleep-adjacent corrupting entry (MECH122-CONTENT-PACKAGING-SPINDLE-SELECTION) touches mel_consumer.py::relative_novelty and agent.py::run_sws_schema_pass, neither of which this driver calls -- it reaches cross_module_consolidation.py directly and builds no SleepLoopManager. | red-team (fable; model diversity from the authoring session's Opus 5): CONTESTED -> 4 findings, ALL verified against source, 3 FIXED and 1 instrumented-not-gated with reason, 0 dismissed silently. F1 cells_ok was inherited as a_rows+b_rows+c_rows and EXCLUDED the new arm, so a non-finite ARM_D cell would have routed to a CLAIM VERDICT ('weakens') instead of instrument-not-ready -- nan<=y and inf<=y are both False so such a seed counts as 'D worse than B', and the non-degeneracy self-check cannot catch it since NaNs are distinct set members; FIXED to all four arms. F2 ARM_D's LAST pass before the probes is the recent-window one, byte-identical to ARM_B's only pass, so a last-writer-wins overwrite would collapse D onto B, make C4 a B-vs-B comparison that passes trivially, and record it as 'the cost was a reallocation artefact'; FIXED with the additive_arm_retains_replay_gain precondition (1048's C1 form with D substituted, same seed-majority constant, no new threshold). F3 C4's premise -- that a matched-budget deficit EXISTS to be explained -- was gated nowhere and is not guaranteed: 1048's recorded metrics.json has n_seeds_a_not_worse_late = 1.0, i.e. C2 was 1/5 NOT 0/5 (one seed's +9.25% excess sat under LATE_TOL); FIXED by giving the premise-absent state its own branch and 'mixed' direction, leaving the pre-registered C4 untouched. F4 the persistence-skill gate covers neither the compared arm (D) nor the compared leg (LATE); accurate but an INHERITED gap (1048's C2 was load-bearing on the same late leg with no such gate, so late skill has never been measured on this harness and there is no basis for a floor) -- NOT gated, INSTRUMENTED instead: per-arm late skill and the per-seed best over D/B are now in the readout so a successor can set one. Widening the existing early gate to include D was considered and REJECTED as a strict WEAKENING (a max over three arms is weaker than the gate 1048 passed). | DECLARED RISK, stated up front so a requeue is not read as a wasted cycle: the F2 gate FIRES at --dry-run scale. Seed 42 toy scale gave ARM_D early-probe MSE 1.45e-4 against ARM_B 1.43e-4 and ARM_A 1.13e-4 -- D retained none of A's early gain -- and the run correctly routed to substrate_not_ready_requeue rather than emitting a verdict. At full scale A's early advantage over B was 45% across 5/5 seeds with 4 consolidation points rather than the dry-run's compressed schedule, so the gate may or may not clear. If it does NOT, that is itself the finding -- the two-pass additive construction does not deliver 'A's benefit plus B's', which a successor needs to know before trying the reverse pass order -- and it is recorded as an instrument state, never as a MECH-017 verdict. The PASS-ORDER question this raises is unspecified by the ratifying autopsy and has been put to the user as decision chip chip-20260918-mech017-arm-d-pass-order rather than decided here. | PROVENANCE: validate_experiments.py --strict clean (0 warnings on all 37 checks) both before and after the red-team fixes; --dry-run smoke run twice, 4/4 arms distinct and bit-stable across the fix, ep N/M denominators correct, no manifest written. ARM_D verified live: extra_steps=32 = A(16)+B(16), updates_e1=16 = 2x A's 8, window=ALL+36, replay_early_share_whole_buffer_pass=0.571 matching ARM_A exactly. Script landed alone first as NOT QUEUED; this entry is the finish."
    }
  ]
}
