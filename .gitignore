__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv
env/
venv/
*.log
runs/
/traces/
.DS_Store
*.swp
*.swo
runner.pid
runner *.pid
script_timing.json
runner_status.tmp
coordinator/deploy/fleet_integrity_baseline.json
# Per-host writer-health snapshot written by sync_daemon (see
# coordinator/app.py /writer-health endpoint, 2026-05-31). Local-only;
# never commit. Untracked-and-not-ignored wedges phase3_queue_writer's
# clean-tree refusal.
coordinator/writer_health.json
# /understand per-machine structural-fingerprints cache written by
# scripts/mcp-tools/build-fingerprints.mjs at the end of Phase 7. Per-clone
# scratch that churns on every regeneration; must NOT be tracked. See the
# nightly-documentation-update scheduled task (Step 2, item 3). Sibling
# knowledge-graph.json + meta.json ARE the consumable graph and stay tracked.
.ua/fingerprints.json
